# Crontab 보기
sudo crontab -l

# Crontab 편집
sudo crontab -e

# Crontab 실행 로그
view /var/log/syslog

# 인증서 갱신 스크립트
0 0 1 * * /home/username/.local/bin/sudo -E python3 certbot certonly --webroot -w /var/www/certbot -d example1.com